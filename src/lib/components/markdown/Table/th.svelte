<th class:th={$$slots['default']}>
	<slot />
</th>

<style>
	.th {
		border-bottom: 1px solid var(--color-border);
		padding: 0.5rem 1rem;
		text-align: left;
		font-size: 0.875rem;
		border-right: 1px solid var(--color-border);

		user-select: none;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-weight: 600;
		color: var(--color-text);
	}
</style>
